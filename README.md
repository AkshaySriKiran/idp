# idp

move the code (app.js, index.html, style.css) to a folder, here i used work as folder ,

to run :

cd C:\work ( directory that you saved the code in folder )
 
python -m http.server 8000

then you can access this tool in web browser at this port: " http://localhost:8000 "
